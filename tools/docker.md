### 平时常用的docker命令

```
###### 全局
# 列出当前运行的docker
docker ps

# 查看某个docker的日志
docker logs --tail 500 -f <docker-id>



###### 在指定的docker目录下
# 启动docker
docker-compose up -d

# 关闭docker
docker-compose stop
```
